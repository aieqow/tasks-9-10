def flip(arr, k):
    """
    Переворачивает первые k элементов списка, как стопку блинов.
    """
    arr[:k] = arr[:k][::-1]
def pancake_sort(arr):
    """
    Блинная сортировка (Pancake Sort).
    Сортируем список, переворачивая его части.
    """
    n = len(arr)
    # Проходим от конца списка к началу
    for size in range(n, 1, -1):
        # 1. Находим индекс максимального элемента в arr[0:size]
        max_index = arr.index(max(arr[0:size]))
        # Если максимальный элемент уже на своем месте — пропускаем
        if max_index == size - 1:
            continue
        # 2. Поднимаем максимум наверх (в начало), перевернув первые max_index+1 элементов
        flip(arr, max_index + 1)
        # 3. Переворачиваем первые size элементов — помещаем максимум в конец
        flip(arr, size)
    return arr
# Пример использования
numbers = [3, 6, 1, 10, 2, 8]
print("Исходный список:", numbers)
print("Отсортированный:", pancake_sort(numbers))
